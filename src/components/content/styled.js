import styled from 'styled-components';

export const StyledRoot = styled.div`
  background-color: #e8e8e8;
  box-sizing: border-box;
  height: calc(100vh - 71px); /* 71px corresponds to Header's height */
  padding: 24px;
  width: 100vw;
`
